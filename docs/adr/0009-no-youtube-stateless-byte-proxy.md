# No YouTube, and the only server left is a stateless byte proxy

Status: accepted, not yet built.

Dropping YouTube import removes yt-dlp. Moving chunking onto byte ranges ([ADR 0010](0010-byte-range-chunks-asr-placed-cuts.md)) removes ffmpeg. What remains needs no disk, no CPU and no state: follow the redirect a podcast host answers with, add the CORS headers it does not send, and hand a transcription endpoint a byte range dressed up as a whole file. That is a Cloudflare Worker which pipes `upstream.body` straight into its Response, so the bytes never enter the isolate and a 26MB slice costs the same CPU as a 200-byte one. Nothing to deploy but a static page and one Worker.

We considered keeping a small Node server for YouTube alone. Rejected: it drags ffmpeg, yt-dlp, a container and a host back in to serve one source, and it re-opens an abuse surface that is otherwise closed — an endpoint that will run yt-dlp against any URL anyone posts is a free transcoding farm, and [ADR 0008](0008-browser-owns-all-state.md) has just removed the password that used to stand in front of it.

The proxy cannot be deleted along with the server, and the reason is not YouTube. Three things a browser cannot do for itself, all observed on real feeds: one podcast host answers browser requests with no CORS headers at all; another answers a 302 that the transcription endpoint refuses to follow, failing with `media_fetch_failed`; and a third splices advertising by User-Agent, answering `curl` with 43,421,257 bytes and a browser with 44,263,131 — a 30-second difference in the same episode. That last one is why every byte must go through the proxy rather than only the awkward ones: a byte offset means nothing across two versions of a file, so one client with one fixed User-Agent has to fetch all of them.

The two halves deploy separately and know nothing about each other. The page is static assets on one Worker — a binding, not a hand-written file server — and the proxy is a second Worker addressed by its base URL and key, both of which are settings the reader fills in like any other. Nothing about the proxy is compiled into the page, so the page can be published anywhere and pointed at a proxy someone runs themselves.

That indirection is also what makes the key worth having. Baked into the bundle it would be a public constant handed to everyone who loads the page, and worth only a rotation lever; entered per reader it is a genuine shared secret distributed out of band, which is the job [ADR 0001](0001-single-tenant-self-hosted.md)'s access password used to do. It is the only gate on who gets served, deliberately: an origin allowlist beside it would refuse nobody the key does not already refuse — a holder passes with or without an `Origin` header, since `curl` simply omits one — while breaking the reader who hosts the page themselves, which is the property the paragraph above just bought. A content-type allowlist stays, because it answers a different question: not who is asking, but what this can be made to fetch.

Known ceilings, none of them fatal: the key is shared rather than per-reader, so revoking one person means rotating for everybody; it rides in a query string, so it reaches the transcription provider's request logs; and it has no expiry. Rate limiting remains the backstop against a scripted abuser who has obtained it, and belongs in Cloudflare's own rules rather than in this code. The proxy never sees an API key — those travel in an `Authorization` header to the provider, or in one the provider sends itself — but it does see which episodes are being fetched.
